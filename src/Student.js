import React, { Component } from 'react'

export default class Student extends Component {
    names = ["Pritesh", "Armen", "Sahay", "Aryan", "Simon"]

    render() {
        return (
            <div>
                {
                    // this.names.map(n => (
                    //     <h2>{n}</h2>
                    // ))
                }
            </div>
        )
    }
}

// export default Student

